% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html.R
\name{html_tag}
\alias{html_tag}
\title{Build an HTML element}
\usage{
html_tag(name, ..., class = NULL, style = NULL, id = NULL)
}
\arguments{
\item{name}{Character: Element name, e.g. "div".}

\item{...}{Children: Character, numbers, \code{rtemis_html} objects, or lists of
those.}

\item{class}{Optional Character: \code{class} attribute.}

\item{style}{Optional Character: \code{style} attribute.}

\item{id}{Optional Character: \code{id} attribute.}
}
\value{
Character of class \code{rtemis_html_element} and \code{rtemis_html}.
}
\description{
Children given as \code{...} are escaped unless marked with \code{\link[=html_raw]{html_raw()}} or
produced by another \verb{html_*} constructor. Lists are flattened, so children
can be built with \code{lapply()}, and NULL children are dropped.
}
\details{
A tag holding a single text child renders on one line; anything else renders
as an indented block, and a child's own line breaks are indented with it, so
indentation always tracks nesting depth.
}
\examples{
html_tag("section", "Body text", class = "intro")
html_tag("ul", lapply(c("one", "two"), html_li))
}
\author{
EDG
}
