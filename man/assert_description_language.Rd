% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema_contract.R
\name{assert_description_language}
\alias{assert_description_language}
\title{Assert a published schema's descriptions are language-independent}
\usage{
assert_description_language(schema, id = schema[["$id"]])
}
\arguments{
\item{schema}{Named list: The generated schema, as \code{S7_to_JSONSchema()} or
\code{S7_dispatcher_JSONSchema()} returns it.}

\item{id}{Character: The schema's \verb{$id}, used to name it in the error.}
}
\value{
The \code{schema}, invisibly, so it can wrap a write call. Throws with
class \code{simpleError} naming every offending description, so one run reports
all of them rather than the first.
}
\description{
The two prose rules of the input-schema contract, on their own: no
description may name an R construct, and none may spell a value the way R
does. \code{assert_config_contract()} applies both along with the rules about
what a config may demand; this is the entry point for a document those do
not govern -- a record, or a result class whose \code{required} states what
rtemis always writes.
}
\details{
Every published document is read by R, by the Rust CLI, by the browser and
by a model that writes no code at all, so its prose is part of the interface
rather than a comment on it. A description reading "NULL = unweighted" is
correct roxygen and an invalid instruction to every reader but one, who must
write \code{null}; the fix is to say what the absent value means, not to
transliterate the literal.
}
\examples{
assert_description_language(
  list(
    type = "object",
    properties = list(k = list(type = "integer", description = "Clusters."))
  ),
  "https://schema.rtemis.org/example/v1/schema.json"
)
}
\author{
EDG
}
