% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_numeric}
\alias{check_numeric}
\title{Check numeric}
\usage{
check_numeric(x, allow_null = TRUE, arg_name = deparse(substitute(x)))
}
\arguments{
\item{x}{Vector to check.}

\item{allow_null}{Logical: If TRUE, NULL values are allowed and return early.}

\item{arg_name}{Character: Name of the variable for error messages.}
}
\value{
Called for side effects. Throws an error if checks fail.
}
\description{
Checks that \code{x} is numeric. Uses \code{is.numeric()}, which accepts both
"double" and "integer" inputs - the right semantics for "is this a
number?". Prefer this over \code{check_inherits(x, "numeric")}, which
rejects integers because their literal class is \code{"integer"}, not
\code{"numeric"} (a long-standing R/S3 quirk). This trips up wire-format
callers: \code{jsonlite::fromJSON("1")} returns an integer, and the value
would then fail \code{inherits(., "numeric")} despite being a perfectly
good number.
}
\examples{
check_numeric(1L)
check_numeric(1.5)
check_numeric(c(1, 2, 3))
# Throws error:
try(check_numeric("1"))
try(check_numeric(TRUE))
}
\author{
EDG
}
