% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_S7_prop_factories.R
\name{prop_float}
\alias{prop_float}
\title{Numeric (floating-point) S7 property}
\usage{
prop_float(
  default = NULL,
  min = NULL,
  max = NULL,
  exclusive_min = NULL,
  exclusive_max = NULL,
  nullable = FALSE,
  vector = FALSE,
  min_items = 1L,
  unique_items = FALSE,
  description = ""
)
}
\arguments{
\item{default}{Numeric: Default value (NULL for none, or if \code{nullable}).}

\item{min, max}{Numeric or NULL: Inclusive bounds.}

\item{exclusive_min, exclusive_max}{Numeric or NULL: Exclusive bounds.}

\item{nullable}{Logical: If TRUE, NULL is a valid value.}

\item{vector}{Logical: If TRUE, the value is vector-valued (a JSON array).}

\item{min_items}{Integer: Fewest elements a \code{vector} value may hold.}

\item{unique_items}{Logical: If TRUE, a \code{vector} value's elements must be
distinct.}

\item{description}{Character: Human-readable description.}
}
\value{
S7 property.
}
\description{
The one factory whose name differs from its JSON Schema type: it emits type
"number" (which in JSON Schema includes integers), but is named \code{prop_float}
because declarers think in the integer/float pairing -- "number" next to
\code{prop_integer} invites the same ambiguity as R's "numeric". Accepts R
integer values too (\code{class_numeric}), floats being a superset of integers.
}
\examples{
# Sampling temperature in [0, 2]
temperature <- prop_float(default = 0.3, min = 0, max = 2)
# Learning rate in (0, 1]
lr <- prop_float(default = 0.1, exclusive_min = 0, max = 1)
}
\author{
EDG
}
