% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema_contract.R
\name{assert_config_contract}
\alias{assert_config_contract}
\title{Assert a generated config schema honors the input-schema contract}
\usage{
assert_config_contract(schema, id = schema[["$id"]], structural = character())
}
\arguments{
\item{schema}{List: The generated schema, as \code{S7_to_JSONSchema()} returns it.}

\item{id}{Character: The schema's \verb{$id}, used to name it in the error.}
}
\value{
The \code{schema}, invisibly. Throws with class \code{simpleError} listing
every rule broken, so one run reports all of them rather than the first.
}
\description{
Checks one generated JSON Schema against the rules a \emph{config} document must
obey, and throws if any is broken. Shared by every package that publishes to
schema.rtemis.org -- rtemis and rtemis.draw -- so one registry cannot hold
documents held to two standards.
}
\details{
Four rules, each recorded where it is raised:
\itemize{
\item No \code{default}: defaults are versioned separately, in \code{defaults/v1}.
\item No conditional demand for a key (\code{then}/\verb{else} with \code{required}, or
\code{dependentRequired}): an implementation could satisfy it by filling a
value, which makes it a resolution rule and belongs to the record form.
\item No R construct named in a description: the corpus is language-independent
and is read by R, by the Rust CLI, by the browser, and by a model that
writes no code at all.
}

Record schemas are not subject to this: a record states what a run used, so
everything in it is required and the config's rules do not apply.
}
\examples{
assert_config_contract(
  list(type = "object", properties = list(k = list(type = "integer"))),
  "https://schema.rtemis.org/example/v1/schema.json"
)
}
\author{
EDG
}
