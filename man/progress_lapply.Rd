% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/progress.R
\name{progress_lapply}
\alias{progress_lapply}
\title{\code{lapply} with progress}
\usage{
progress_lapply(
  x,
  fn,
  ...,
  label = "Processing",
  kind = "progress",
  verbosity = NULL,
  package = NULL,
  output_type = NULL
)
}
\arguments{
\item{x}{Vector or list: Elements to iterate over, as in \code{\link[=lapply]{lapply()}}.}

\item{fn}{Function: Applied to each element of \code{x}.}

\item{...}{Additional arguments passed to \code{fn}.}

\item{label}{Character: Display label for the progress node.}

\item{kind}{Character: Node kind forwarded in the sink envelope.}

\item{verbosity}{Integer or NULL: Overrides \code{get_verbosity()} when
supplied. Gates only the console renderer, never sink events.}

\item{package}{Character or NULL: Package name for verbosity override
lookup.}

\item{output_type}{Character or NULL: \code{"ansi"}, \code{"html"}, or \code{"plain"};
resolved via \code{\link[=get_output_type]{get_output_type()}} when NULL.}
}
\value{
List: Exactly what \code{lapply(x, fn, ...)} returns.
}
\description{
Drop-in replacement for \code{\link[=lapply]{lapply()}} that reports progress after each
element: begins a progress node, ticks once per element, and ends the
node. Nested calls produce a nested breadcrumb on the console (and
\code{parent_id}-chained sink events). If \code{fn} throws, the node is ended with
\code{status = "error"} before the condition propagates, so nested wrappers
unwind cleanly.
}
\examples{
sqrts <- progress_lapply(
  1:4,
  sqrt,
  label = "Computing",
  output_type = "plain"
)
}
\seealso{
Other progress:
\code{\link[=progress_begin]{progress_begin()}},
\code{\link[=progress_end]{progress_end()}},
\code{\link[=progress_update]{progress_update()}}
}
\author{
EDG
}
\concept{progress}
