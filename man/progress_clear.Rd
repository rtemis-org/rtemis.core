% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/progress.R
\name{progress_clear}
\alias{progress_clear}
\title{Clear the visible progress status line}
\usage{
progress_clear()
}
\value{
NULL invisibly.
}
\description{
Escape hatch for code that is about to write raw console output (e.g.
\code{cat()}, \code{print()}, or a verbose third-party routine) while a progress
status line is on screen: clears the line so the output starts clean.
The status line reappears on the next \code{\link[=progress_update]{progress_update()}}. No-op when
nothing is displayed.
}
\details{
Not needed for output that goes through \code{msg()} and the other rtemis
writers (they clear automatically), nor for \code{message()}/\code{warning()}
conditions raised inside \code{\link[=progress_lapply]{progress_lapply()}} (a calling handler clears
for those). This exists for the one gap that cannot be intercepted:
direct writes to stdout.
}
\examples{
progress_clear() # no-op when no status line is visible
}
\seealso{
Other progress:
\code{\link[=progress_begin]{progress_begin()}},
\code{\link[=progress_end]{progress_end()}},
\code{\link[=progress_lapply]{progress_lapply()}},
\code{\link[=progress_update]{progress_update()}}
}
\author{
EDG
}
\concept{progress}
