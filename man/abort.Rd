% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log.R
\name{abort}
\alias{abort}
\title{Dual-channel error signal}
\usage{
abort(..., class = NULL, parent = NULL, verbosity = NULL, package = NULL)
}
\arguments{
\item{...}{Message components, concatenated with no separator.}

\item{class}{Character vector: Additional condition classes (prepended
to the base \code{c("rtemis_error", "error", "condition")}).}

\item{parent}{Condition or NULL: Wrapped parent condition. Its message is
echoed to the console (when verbosity allows) and stored on the
signalled condition as \verb{$parent}.}

\item{verbosity}{Integer or NULL: Overrides \code{get_verbosity()} for the
console echo. Set to \code{0L} to suppress the echo without affecting the
signalled condition.}

\item{package}{Character or NULL: Package name for verbosity override.}
}
\value{
Does not return - always signals a condition via \code{stop()}.
}
\description{
Signals a condition AND optionally writes a styled error message to the
operator console. The condition's \code{message} field is \strong{plain text} with
all ANSI escapes stripped, so it is safe to serialize into JSON, HTML, or
any other ANSI-unaware sink (e.g. browser-side error display).
}
\details{
Use \code{class} to add wire-protocol-specific condition classes that callers
can catch via \code{tryCatch()}. The base classes \code{"rtemis_error"}, \code{"error"},
and \code{"condition"} are always added.
}
\examples{
\dontrun{
abort("Could not parse ", "hyperparameters", ".",
      class = "rtemislive_invalid_params")
}
}
\author{
EDG
}
