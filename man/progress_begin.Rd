% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/progress.R
\name{progress_begin}
\alias{progress_begin}
\title{Begin a progress node}
\usage{
progress_begin(
  total,
  label = "Progress",
  kind = "progress",
  id = NULL,
  verbosity = NULL,
  package = NULL,
  output_type = NULL
)
}
\arguments{
\item{total}{Integer: Total number of steps, or \code{NA} for indeterminate
progress. Must be non-negative.}

\item{label}{Character: Display label for this progress node.}

\item{kind}{Character: Node kind forwarded in the sink envelope (e.g.
\code{"tune"}, \code{"grid_cell"}).}

\item{id}{Character or NULL: Node id for the sink envelope. Auto-generated
(\code{"pb1"}, \code{"pb2"}, ...) when NULL; pass an execution-graph node id to
correlate progress with other node events.}

\item{verbosity}{Integer or NULL: Overrides \code{get_verbosity()} when
supplied. Gates only the console renderer, never sink events.}

\item{package}{Character or NULL: Package name for verbosity override
lookup (e.g. \code{"rtemis"}).}

\item{output_type}{Character or NULL: \code{"ansi"}, \code{"html"}, or \code{"plain"};
resolved via \code{\link[=get_output_type]{get_output_type()}} when NULL (\code{"ansi"} in interactive
sessions, \code{"plain"} otherwise). Only \code{"ansi"} animates.}
}
\value{
An \code{rtemis_progress} handle (environment), invisibly. Pass it to
\code{\link[=progress_update]{progress_update()}} and \code{\link[=progress_end]{progress_end()}}.
}
\description{
Creates a progress handle, pushes it on the active-progress stack (nesting
under the current innermost handle, if any), emits a \code{status = "start"}
sink event when a message sink is set (see \code{\link[=set_msg_sink]{set_msg_sink()}}), and renders
the console status line otherwise.
}
\details{
The console renderer writes a single status line rewritten in place, with
a color-pulsing spinner and a breadcrumb of all active levels, e.g.
\verb{Outer resamples 2/5 > Tuning 7/30 ETA 0:41}. Non-\code{"ansi"} output gets one
begin line and one completion line instead (no line rewriting). Two
options control rendering:
\itemize{
\item \code{rtemis.progress_throttle}: Minimum seconds between redraws and between
sink \code{"update"} events (default \code{0.1}). Begin/end events always fire;
set to \code{0} to emit every tick.
\item \code{rtemis.progress_spinner}: Spinner design - one of \code{"dots"} (braille
dots, default), \code{"dot"} (static dot, color-only animation), or
\code{"blocks"} (quadrant blocks). All designs share the palette color pulse
(\code{light_orange} to \code{red}).
}

Sink events fire regardless of verbosity; verbosity gates only the
console renderer.

\strong{Known limitation}: output written directly to stdout by code running
inside a progress loop (\code{cat()}, \code{print()}, verbose third-party
routines) is not intercepted and will collide with the status line.
Output through \code{msg()} and the other rtemis writers clears the line
automatically, and \code{\link[=progress_lapply]{progress_lapply()}} additionally clears it for any
\code{message()}/\code{warning()} condition raised by user code. For raw stdout
writes, call \code{\link[=progress_clear]{progress_clear()}} first or silence the routine
(\code{capture.output()}, \code{verbose = 0}).
}
\examples{
h <- progress_begin(3L, label = "Fitting", output_type = "plain")
progress_update(h)
progress_end(h)
}
\seealso{
Other progress:
\code{\link[=progress_clear]{progress_clear()}},
\code{\link[=progress_end]{progress_end()}},
\code{\link[=progress_lapply]{progress_lapply()}},
\code{\link[=progress_update]{progress_update()}}
}
\author{
EDG
}
\concept{progress}
