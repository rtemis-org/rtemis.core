% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_bounded_integer_scalar}
\alias{check_bounded_integer_scalar}
\title{Check integer scalar within bounds}
\usage{
check_bounded_integer_scalar(
  x,
  lower = -Inf,
  upper = Inf,
  arg_name = deparse(substitute(x))
)
}
\arguments{
\item{x}{Numeric: Value to check. Must be a single non-NA whole number.}

\item{lower}{Numeric scalar: Lower bound, inclusive. Default \code{-Inf}.}

\item{upper}{Numeric scalar: Upper bound, inclusive. Default \code{Inf}.}

\item{arg_name}{Character: Argument name to use in error messages.}
}
\value{
Called for side effects. Throws an error if checks fail.
}
\description{
Check integer scalar within bounds
}
\details{
For bounds not covered by the fixed-range checks: \code{check_pos_integer_scalar()} is
\verb{[1, Inf)}, and there is no non-negative or upper-bounded equivalent. Integer-typed
inputs (\code{5L}) and double-typed whole numbers (\code{5}) are both accepted, matching
\link{check_integer_scalar}. Both bounds are inclusive; an exclusive integer bound is the
next whole number in.
}
\examples{
check_bounded_integer_scalar(5L, lower = 0, upper = 20)
check_bounded_integer_scalar(0, lower = 0)
# Throw error:
try(check_bounded_integer_scalar(25, lower = 0, upper = 20))
try(check_bounded_integer_scalar(1.5, lower = 0))
}
\author{
EDG
}
