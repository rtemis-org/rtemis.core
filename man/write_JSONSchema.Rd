% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{write_JSONSchema}
\alias{write_JSONSchema}
\title{Write a JSON Schema to file}
\usage{
write_JSONSchema(schema, file, overwrite = FALSE, digits = NA, verbosity = 1L)
}
\arguments{
\item{schema}{Named list: The schema.}

\item{file}{Character: Path to output JSON file.}

\item{overwrite}{Logical: If TRUE, overwrite an existing file.}

\item{digits}{Integer or NA: Significant digits for numeric values, passed to
\code{jsonlite::toJSON()}. \code{NA} writes each number at full precision as R prints
it. Pass \code{I(17)} where a value must round-trip an IEEE 754 double exactly:
jsonlite's own default is 4 \emph{decimal places}, which silently rounds, and a
document that is nearly right is worse than one that is obviously wrong.}

\item{verbosity}{Integer: Verbosity level.}
}
\value{
\code{schema}, invisibly.
}
\description{
Serializes a schema built as a named list and writes it, with the keywords in
the registry's reading order. Every generator that publishes to
schema.rtemis.org writes through this, so the documents share one shape.
}
\examples{
\dontshow{if (requireNamespace("jsonlite", quietly = TRUE)) withAutoprint(\{ # examplesIf}
schema <- list(
  `$schema` = "https://json-schema.org/draft/2020-12/schema",
  `$id` = "https://example.org/demo/v1/schema.json",
  title = "Demo",
  type = "object",
  properties = list(n = list(type = "integer", minimum = 1L))
)
tmpfile <- file.path(tempdir(), "demo.schema.json")
write_JSONSchema(schema, tmpfile, overwrite = TRUE, verbosity = 0L)
\dontshow{\}) # examplesIf}
}
\author{
EDG
}
